services:
  app:
    build: .
    container_name: intelligent-app
    ports:
      - "8000:8000"
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:7
    container_name: intelligent-redis
    ports:
      - "6379:6379"